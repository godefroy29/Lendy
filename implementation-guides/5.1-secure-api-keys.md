# 5.1 Secure API Keys

**Priority:** High  
**Category:** Security & Privacy  
**Status:** ❌ Not Implemented  
**Difficulty:** 6 ⭐⭐⭐⭐⭐⭐  
**Modification Location:** Flutter Code + Config

## Description

Move Supabase credentials to environment variables or secure storage.

## Implementation Details

- Use `flutter_dotenv` package for environment variables
- Create `.env` file (add to `.gitignore`)
- Move Supabase URL and anon key to environment variables
- Create `.env.example` file with placeholder values
- Never commit actual credentials to version control
- Use different credentials for dev/staging/production

## Files to Modify

- **Modify:** `lendy/lib/config/supabase_config.dart` (read from env)
- **Create:** `.env` and `.env.example` files
- **Modify:** `.gitignore` (add .env)

## Dependencies

Add `flutter_dotenv: ^5.1.0` package

## Implementation Steps

1. Add `flutter_dotenv` package to `pubspec.yaml`
2. Create `.env` file with Supabase credentials
3. Create `.env.example` with placeholder values
4. Add `.env` to `.gitignore`
5. Update `supabase_config.dart` to read from environment variables
6. Load `.env` file in `main.dart`
7. Use different `.env` files for different environments
8. Never commit actual credentials
