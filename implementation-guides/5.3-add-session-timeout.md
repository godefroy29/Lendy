# 5.3 Add Session Timeout

**Priority:** Low  
**Category:** Security & Privacy  
**Status:** ❌ Not Implemented  
**Difficulty:** 6 ⭐⭐⭐⭐⭐⭐  
**Modification Location:** Flutter Code

## Description

Automatically log out users after period of inactivity.

## Implementation Details

- Track user activity (last interaction time)
- Set configurable timeout duration (e.g., 15 minutes)
- Show warning before timeout (e.g., 1 minute before)
- Log out user and clear sensitive data on timeout
- Allow user to extend session
- Make timeout optional in settings

## Files to Create

- **Create:** `lendy/lib/services/session_service.dart`

## Files to Modify

- **Modify:** `lendy/lib/presentation/screens/auth_wrapper.dart` (check session)

## Dependencies

None

## Implementation Steps

1. Create `SessionService` to track user activity
2. Track last interaction time
3. Check session timeout in `AuthWrapper`
4. Show warning dialog before timeout
5. Log out user on timeout
6. Add session extension option
7. Make timeout configurable in settings
8. Test session timeout functionality
